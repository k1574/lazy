scsm
========

Preprocessing
-------------
The scms allows you to put any output of commands like that:
	<{system date}\>
So output will look like:
	<{system date}>

Or we can do something cool like that:
<{system figlet scms is cool | awk '{print "	" $0}'}>
(If you use pp of course)

